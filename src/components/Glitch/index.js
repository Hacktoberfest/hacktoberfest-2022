import { StyledGlitch } from './Glitch.styles';

const Glitch = ({ image }) => (
  <StyledGlitch>
    {image}
    <span>
      {image}
      {image}
    </span>
  </StyledGlitch>
);

export default Glitch;
