export const prWaitingTime = 7 * 24 * 60 * 60 * 1000;

/**
 * Used for:
 *   - Homepage countdown
 *   - Displaying start hacking in nav
 *   - Controlling access to auth hook
 *   - Controlling access to registration page
 */
export const registrationStart = '2022-09-26T18:00:00Z';

/**
 * Used for:
 *   - Controlling access to registration page
 */
export const registrationEnd = '2022-11-01T12:00:0Z';

/**
 * Used for:
 *  - Toggling no PR/MR messaging on profile
 */
export const trackingStart = '2022-09-30T10:00:00Z';

/**
 * Used for:
 *   - Displaying start hacking in nav
 *   - Controlling access to auth hook
 */
export const profileEnd = '2022-12-15T12:00:0Z';
